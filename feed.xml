<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://mingyongma-agent-design.pages.dev/feed.xml" rel="self" type="application/atom+xml"/><link href="https://mingyongma-agent-design.pages.dev/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-24T23:30:03+00:00</updated><id>https://mingyongma-agent-design.pages.dev/feed.xml</id><title type="html">Mingyong Ma</title><subtitle>Personal portfolio and blog of Mingyong Ma. </subtitle><entry><title type="html">From Prompt Chains to Agent Graphs: How Our Agent Architecture Evolved</title><link href="https://mingyongma-agent-design.pages.dev/blog/2026/from-loops-to-graphs-my-journey-building-ai-agents/" rel="alternate" type="text/html" title="From Prompt Chains to Agent Graphs: How Our Agent Architecture Evolved"/><published>2026-09-13T22:45:00+00:00</published><updated>2026-09-13T22:45:00+00:00</updated><id>https://mingyongma-agent-design.pages.dev/blog/2026/from-loops-to-graphs-my-journey-building-ai-agents</id><content type="html" xml:base="https://mingyongma-agent-design.pages.dev/blog/2026/from-loops-to-graphs-my-journey-building-ai-agents/"><![CDATA[<h3 id="stage-1---build-chatbot-using-prompt-chaining-workflow">Stage 1 - Build chatbot using prompt chaining workflow</h3> <p>The first iteration began in early 2025, when we frequently had to diagnose batch-ingestion failures. Much of the troubleshooting process was repetitive, so I started wondering whether we could build a chatbot to automate parts of the diagnosis and reduce the amount of manual effort involved.</p> <p>At the time, we still thought of the system primarily as a chatbot rather than an agent.</p> <p>I built a <code class="language-plaintext highlighter-rouge">prompt-chaining</code> workflow using <a href="https://crewai.com/">CrewAI</a> and selected Llama 3, served locally through Ollama:</p> <p><img src="/assets/img/uploads/Screenshot%202026-09-13%20at%2011.01.39%20PM.png" alt="" title="Example prompt chaining workflow"/></p> <p>Compared with today’s models, the models available to us at the time were less reliable at multi-step reasoning and following complex instructions. To make the system more predictable and reduce the risk of hallucinations, we kept the workflow largely deterministic: each step had a predefined responsibility, and the execution path was explicitly orchestrated rather than decided dynamically by the model.</p> <h3 id="stage-2--langgraph-based-tool-orchestration">Stage 2 — LangGraph-Based Tool Orchestration</h3> <p>The second iteration began in October 2025. At the time, we had more than 100 tables in Databricks Unity Catalog spanning different commerce domains, such as contracts, licenses, orders, and offers. We wanted to build a chatbot that could answer users’ questions across these datasets.</p> <p>Initially, we considered adopting <a href="https://docs.databricks.com/aws/en/genie/">Databricks Genie</a>. For those unfamiliar with Genie, it provides a natural-language interface for querying data in Databricks. You can create a Genie space, add relevant tables, and provide business context through instructions and example SQL queries to improve the accuracy of the generated queries.</p> <p><img src="/assets/img/uploads/Screenshot%202026-09-14%20at%204.10.19%20PM.png" alt="" title="Genie Interface"/></p> <p>However, after some experimentation, I found two challenges:</p> <ul> <li>As the semantic scope expanded across increasingly heterogeneous business domains, it became harder for a single Genie space to consistently apply the right business context. Adding more instructions did not necessarily solve the problem and sometimes made query generation less reliable.</li> <li>With more than 100 tables across different domains, selecting the correct tables and distinguishing between similar concepts became increasingly difficult.</li> </ul> <p>Rather than putting heterogeneous business domains behind a single generalist Genie space, we partitioned the system into multiple domain-specialized spaces, with each one responsible for a narrower business area.</p> <p><img src="/assets/img/uploads/ChatGPT%20Image%20Sep%2024%2C%202026%2C%2004_15_54%20PM.png" alt="" title="Single generalist vs. domain-specialized Genie spaces"/></p> <p>In our internal testing, this domain specialization reduced table-selection errors, but it introduced a new problem: <strong>how should we route each request to the appropriate Genie space?</strong></p> <p>At the time, I was inspired by the way Cursor integrated with MCP servers: external systems could expose capabilities as tools, and Cursor’s agent could decide when to invoke them. That led me to a similar design idea: what if we wrapped each domain-specific Genie API as a tool and placed an LLM-based orchestrator in front of them?</p> <p>This is what the architecture looked like:</p> <p><img src="/assets/img/uploads/ChatGPT%20Image%20Sep%2016%2C%202026%2C%2004_02_49%20PM.png" alt="" title="LLM orchestrator with domain-specialized Genie tools"/></p> <p>We implemented this orchestration layer with LangGraph. At its core, the graph formed a simple model–tool loop: the model decided whether a tool call was needed, LangGraph executed the selected tool, and the result was fed back to the model for the next step.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langgraph.graph</span> <span class="kn">import</span> <span class="n">END</span><span class="p">,</span> <span class="n">StateGraph</span>
<span class="n">workflow</span> <span class="o">=</span> <span class="nc">StateGraph</span><span class="p">(</span><span class="n">AgentState</span><span class="p">)</span>

<span class="n">workflow</span><span class="p">.</span><span class="nf">add_node</span><span class="p">(</span><span class="sh">"</span><span class="s">agent</span><span class="sh">"</span><span class="p">,</span> <span class="nc">RunnableLambda</span><span class="p">(</span><span class="n">call_model</span><span class="p">))</span>
<span class="n">workflow</span><span class="p">.</span><span class="nf">add_node</span><span class="p">(</span><span class="sh">"</span><span class="s">tools</span><span class="sh">"</span><span class="p">,</span> <span class="nc">ToolNode</span><span class="p">(</span><span class="n">tools</span><span class="p">))</span>

<span class="n">workflow</span><span class="p">.</span><span class="nf">set_entry_point</span><span class="p">(</span><span class="sh">"</span><span class="s">agent</span><span class="sh">"</span><span class="p">)</span>
<span class="n">workflow</span><span class="p">.</span><span class="nf">add_conditional_edges</span><span class="p">(</span>
  <span class="sh">"</span><span class="s">agent</span><span class="sh">"</span><span class="p">,</span>
  <span class="n">should_continue</span><span class="p">,</span>
  <span class="p">{</span>
     <span class="sh">"</span><span class="s">continue</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">tools</span><span class="sh">"</span><span class="p">,</span>
     <span class="sh">"</span><span class="s">end</span><span class="sh">"</span><span class="p">:</span> <span class="n">END</span><span class="p">,</span>
  <span class="p">},</span>
<span class="p">)</span>
<span class="n">workflow</span><span class="p">.</span><span class="nf">add_edge</span><span class="p">(</span><span class="sh">"</span><span class="s">tools</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">agent</span><span class="sh">"</span><span class="p">)</span>

<span class="k">return</span> <span class="n">workflow</span><span class="p">.</span><span class="nf">compile</span><span class="p">()</span>
</code></pre></div></div> <p>We later integrated the agent with Slack, allowing users to ask data questions directly from their existing workflow.</p> <h3 id="stage-3--building-an-agent-with-a-skill-based-agent-loop">Stage 3 — Building an Agent with a Skill-Based Agent Loop</h3> <p>By early 2026, the idea of packaging reusable knowledge and capabilities into <code class="language-plaintext highlighter-rouge">skills</code> was becoming increasingly common in agent systems. As our use cases expanded, we decided to evolve the architecture again.</p> <p>There were two main motivations:</p> <ul> <li>We wanted the agent to do more than generate SQL. It now needed to triage on-call alerts, investigate pipeline lag, create reporting schedules, generate reports, and support other operational workflows. As the scope grew, we needed a structured and version-controlled way to organize domain knowledge, procedures, and tools.</li> <li>We also wanted that knowledge to improve continuously as engineers worked. Rather than repeatedly encoding the same context into prompts, we started maintaining persistent Markdown-based knowledge that the agent could update over time and later reuse as part of its skills.</li> </ul> <p>Andrej Karpathy’s <a href="https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f">llm-wiki</a> later reinforced this idea: instead of rebuilding context from scratch for every request, an LLM can incrementally maintain a persistent knowledge base that becomes richer over time.</p> <p>Our architecture looks like:</p> <p><img src="/assets/img/uploads/ChatGPT%20Image%20Sep%2017%2C%202026%2C%2004_59_38%20PM.png" alt="" title="Skill based agent loop architecture diagram"/></p> <p>We made a few changes:</p> <ul> <li>Our knowledge will be stored in skill, and the skill.md looks like: <img src="/assets/img/uploads/ChatGPT%20Image%20Sep%2024%2C%202026%2C%2004_28_50%20PM.png" alt="" title="Example skill format"/>Each <code class="language-plaintext highlighter-rouge">SKILL.md</code> defined the instructions, domain knowledge, and tools available for a particular class of tasks.</li> <li>We refactored our MCP-based tools into CLI-backed capabilities organized through the filesystem. We did it since LLM is great at navigating filesystems. Presenting tools as code on a filesystem allows models to read tool definitions on-demand, rather than reading them all up-front.</li> <li>We remove LangGraph from the agent runtime. since they often create extra layers of abstraction.</li> </ul> <h3 id="shifting-from-loop-to-graph">Shifting from loop to graph</h3> <p>We decided to migrate our harness from loop to graph because we find that sometimes there is a dependency via tool selection, but don’t want to hard-code everything in skill.md.</p> <p>Let me explain more, for example, when we want to query a table, we want our agent to describe the table first, check the schema before actual query the table. Oncall is a more complex usecase, we want our agent to first lookup runbook, check cortex, pods to get more knowledge before doing more heavy lifting work such as querying splunk, replay the api call …</p> <p>So there are actual dependencies via tool, but we don’t want to hard-code it in our skill. Because we want to give llm more freedom (llm is getting more and more intelligent), and hard-code everything means if anything changes, we have to update the skill, which is also time-consuming.</p> <p>Therefore, we decided to migrate our agent harness from loop to graph, so instead of defining the <code class="language-plaintext highlighter-rouge">allowed_tools</code> in agent.md file, we will also provide the dependency in the skill:</p> <p><img src="/assets/img/uploads/ChatGPT%20Image%20Sep%2022%2C%202026%2C%2007_43_53%20PM.png" alt="" title="skill with tool dependency"/></p> <p>And we also setup some rules:</p> <ul> <li>If tool A depends on tool B, then tool B will have to be execute first.</li> <li>If all the predecessor has executed, the tool can be use.</li> <li>Every layer of the graph cannot have tool which have predecessor tool not processed yet.</li> </ul> <p>Instead of hard-coding the sequence in skill, we only set the <code class="language-plaintext highlighter-rouge">tool dependency</code> in skill, in that way, llm can reuse the tool as long as it meet with our rules, since each layer it doesn’t have tool dependency, each layer can run tool asynchronously.</p> <p>So this will translate into a DAG, where each node represents a tool, and each edge represents the dependency of the tool. Then a very popular algorithm came into my mind: Topological sort. We will use <code class="language-plaintext highlighter-rouge">Kahn's topological sort</code> algorithm, where a tool will be released for llm where all the predecessor has been used.</p> <p><img src="/assets/img/uploads/ChatGPT%20Image%20Sep%2022%2C%202026%2C%2007_39_01%20PM.png" alt="" title="Loop vs Graph - code comparison"/></p> <figure class="video"> <video controls="" preload="metadata" src="/assets/img/uploads/Untitled%20-%20September%2023%2C%202026%20at%2015.31.16%20%28720p%29.mp4"></video> <figcaption>This video visualize the agent graph when I ask our agent to traige an alert</figcaption> </figure>]]></content><author><name></name></author><summary type="html"><![CDATA[Over the past two years, our agent architecture evolved through four stages: deterministic prompt chains, LangGraph-based orchestration, a lightweight skill-based agent loop, and finally agent graph. This post walks through that evolution and explains why we eventually chose to use a graph to represent our agent orchestration.]]></summary></entry></feed>